import React from 'react';

const MyButton = (props) => {

    return (
        <div>
            
            <button onClick={()=>{}}>my button</button>
        </div>
    );
};

export default MyButton;